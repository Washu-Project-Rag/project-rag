{"id": "q016", "gold_answer": "Multithreading uses multiple threads within the same process, so threads share the same memory space and can communicate quickly, but they also require careful synchronization. Multiprocessing uses multiple separate processes, each with its own memory space, which provides better isolation and can improve parallelism on multiple CPU cores, but inter-process communication is usually more expensive."}
{"id": "q017", "gold_answer": "Authentication verifies who a user or system is, while authorization determines what that authenticated user or system is allowed to access or do. In other words, authentication confirms identity first, and authorization applies permissions afterward."}
{"id": "q018", "gold_answer": "A binary search tree stores data in sorted order based on key comparisons, which supports ordered operations such as range queries and in-order traversal. A hash table stores data using a hash function that maps keys to positions, which usually allows very fast exact lookups but does not maintain sorted order efficiently."}
{"id": "q019", "gold_answer": "Binary search works on a sorted sequence by repeatedly comparing the target value with the middle element. If the target is smaller, the search continues in the left half; if larger, it continues in the right half. This process repeats until the target is found or the search interval becomes empty."}
{"id": "q020", "gold_answer": "DNS resolution begins when a client asks a resolver for the IP address of a domain name. If the answer is not already cached, the resolver queries DNS servers hierarchically, typically starting from the root server, then the top-level domain server, and then the authoritative name server for the domain. The authoritative server returns the requested record, and the resolver sends the result back to the client and may cache it for future use."}
{"id": "q021", "gold_answer": "A database index speeds up lookups by maintaining an auxiliary data structure, such as a B-tree or hash-based structure, that helps the database locate rows without scanning the entire table. Instead of checking every row, the system uses the index to narrow down the search quickly and then retrieve the matching records."}
{"id": "q022", "gold_answer": "Context switching happens when the operating system stops running one process or thread and switches the CPU to another. First, the OS saves the current execution state of the running task, including the program counter, CPU registers, stack pointer, and sometimes memory-management information, into its process or thread control block. Then the scheduler selects another ready task to run. The OS restores that task’s saved state, updates memory mappings if switching between processes, and resumes execution from where that task previously stopped. Context switching allows multitasking and responsiveness, but it adds overhead because saving and restoring state uses CPU time and may disrupt cache locality."}
{"id": "q023", "gold_answer": "Public-key encryption enables secure communication by giving each user a key pair: a public key that can be shared openly and a private key that is kept secret. A sender can encrypt a message, or more commonly a temporary symmetric session key, using the recipient’s public key, and only the recipient’s private key can decrypt it. This allows two parties to exchange secrets without first sharing a private key over the network. Public-key cryptography can also provide authentication and integrity through digital signatures: the sender signs data with a private key, and others verify the signature with the corresponding public key. In practice, systems often use public-key cryptography to establish trust and exchange a symmetric key, then use symmetric encryption for the main data transfer because it is faster."}
{"id": "q024", "gold_answer": "A load balancer improves the reliability of distributed systems by distributing incoming requests across multiple servers instead of sending all traffic to a single machine. This prevents overload on any one server and reduces the chance that a single failure will take down the service. If one backend becomes slow, unhealthy, or unavailable, the load balancer can detect that through health checks and stop routing traffic to it, sending requests to healthy instances instead. It can also support redundancy, failover, and scaling by allowing new servers to be added or removed with less disruption. As a result, load balancers increase availability, improve fault tolerance, and help the system continue serving users even when traffic spikes or individual components fail."}
{"id": "q025", "gold_answer": "Adding an index can improve read performance because an index provides a faster way to locate rows without scanning the entire table. It organizes key values so queries such as searches, joins, and range lookups can find matching records with fewer disk accesses and less work. However, indexes can hurt write performance because every insert, update, or delete may also require updating one or more index structures in addition to the table data itself. This adds CPU, memory, and I/O overhead, and updates may cause index pages to split or be reorganized. Indexes also consume extra storage. Therefore, indexes create a trade-off: they often make reads faster but can slow down writes and increase maintenance cost."}
{"id": "q026", "gold_answer": "Deadlock is more likely in concurrent systems with multiple shared resources because different processes or threads may acquire resources in different orders and then wait for one another. For example, one thread may hold resource A and wait for B, while another holds B and waits for A. As the number of shared resources grows, the number of possible lock-acquisition orders and interdependencies also grows, making circular wait conditions more likely. Deadlock typically occurs when four conditions hold: mutual exclusion, hold-and-wait, no preemption, and circular wait. Systems with many locks, devices, or shared objects therefore face a higher risk unless they use prevention strategies such as lock ordering, timeouts, or deadlock detection."}
{"id": "q027", "gold_answer": "UDP may be preferred over TCP in real-time applications because it has lower overhead and avoids delays caused by connection setup, retransmissions, in-order delivery guarantees, and congestion-control behavior that can increase latency. In applications such as voice calls, video streaming, online gaming, and real-time sensor updates, receiving the newest data quickly is often more important than receiving every packet perfectly. A lost or late packet may be less harmful than waiting for retransmission, because delayed data can become useless in real time. UDP allows applications to send messages immediately and handle timing, loss tolerance, or error recovery at the application level if needed. The trade-off is that UDP does not guarantee delivery, ordering, or reliability by itself."}
{"id": "q028", "gold_answer": "Caching and load balancing work together by reducing how much work the backend servers must do and then distributing the remaining work efficiently. Caching stores frequently requested data closer to users or in faster storage, such as browser caches, CDNs, reverse proxies, or in-memory application caches, so repeated requests can be served quickly without recomputing results or querying the database each time. This lowers latency and reduces backend load. Load balancing then spreads incoming requests across multiple servers so no single server becomes a bottleneck and traffic spikes can be handled more smoothly. Together, caching reduces the total number of expensive requests, while load balancing distributes the requests that still need backend processing, improving both performance and scalability."}
{"id": "q029", "gold_answer": "Distributed systems often trade off consistency, availability, and partition tolerance because of the CAP principle. When a network partition occurs, meaning some nodes cannot communicate reliably with others, the system cannot always guarantee both strong consistency and full availability at the same time. If it chooses consistency, it may reject or delay some requests until replicas can agree, reducing availability. If it chooses availability, it may continue serving requests from different nodes even when they are temporarily out of sync, reducing consistency. Partition tolerance is usually necessary in real distributed systems because network failures and delays are unavoidable. As a result, system designers choose different trade-offs depending on application needs, such as whether it is more important to always return the latest correct data or to always keep the service responsive."}
{"id": "q030", "gold_answer": "Multithreading is often more efficient than multiprocessing when tasks need lightweight concurrency, share memory frequently, or spend much of their time waiting for I/O such as network, disk, or user input. Threads within the same process can communicate more easily and have lower creation and context-switch overhead than separate processes. This makes multithreading attractive for I/O-bound workloads and shared-state applications. Multiprocessing is often more efficient when tasks are CPU-bound and can run independently in parallel, especially on multi-core systems. Separate processes avoid many issues of shared-memory synchronization and can bypass limitations such as a global interpreter lock in some language runtimes. The trade-off is that processes are heavier, use more memory, and require more expensive inter-process communication. In summary, multithreading is usually better for lightweight shared-memory or I/O-bound concurrency, while multiprocessing is usually better for heavy CPU-bound parallel computation and stronger isolation."}